import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  index: 'Overview',
  fetch: 'Fetch',
  serve: 'CDP server',
  mcp: 'MCP',
  agent: 'Agent',
  'common-options': 'Common options',
}

export default meta
