import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  cdp: {
    title: 'CDP',
    theme: {
      collapsed: false,
    },
  },
  mcp: 'MCP',
  api: 'HTTP API',
}

export default meta
